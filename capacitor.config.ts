import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.beatandcode.marhaba',
  appName: 'e-menu-marhaba',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
